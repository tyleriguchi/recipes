<form {{action "create" this on="submit"}}>
  <label>Title: {{input value=title}}</label>
  <label>Description: {{textarea class="description-textarea" value=description}}</label>

  <fieldset>
    <legend>Ingredients</legend>
    <ul>
      <li>
        <label>
          {{input type=text value=ingredientItem}}
        </label>
      </li>
    </ul>
  </fieldset>

  <fieldset>
    <legend>Steps</legend>
    <ol>
      <li>
        <label>
          {{input type=text value=stepItem}}
        </label>
      </li>
    </ol>
  </fieldset>
  <button>Save</button>
</form>
