
  <h2>{{title}}</h2>

  <span class="recipe--text">Description:&emsp;{{description}}</span>

  <ul>Ingredients
  {{#each ingredient in ingredients}}
    <li>{{ingredient.size}}, {{ingredient.item}}</li>
  {{/each}}
  </ul>
  <ol>Directions
  {{#each step in steps}}
    <li>{{step.item}}</li>
  {{/each}}
  </ol>
  <button {{action "edit"}}>Edit</button>
  <button {{action "delete"}}>Delete</button>
  {{outlet}}
